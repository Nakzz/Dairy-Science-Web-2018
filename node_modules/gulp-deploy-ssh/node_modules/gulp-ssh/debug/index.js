'use strict'

var gulp = require('gulp')
var GulpSSH = require('../index')

var gulpSSH = new GulpSSH({
  ignoreErrors: false,
  sshConfig: {
    host: '192.168.1.10',
    port: 22,
    username: 'jarvis',
    password: 'huangjiaju'
  }
})

gulp.task('dest', function () {
  return gulp.src('./node_modules/**')
    .pipe(gulpSSH.dest('/home/jarvis/test'))
})
